# Ignore .git folder
.git
.gitignore

# Ignore IDE and editor specific files
.idea/
.vscode/
*.iml

# Ignore local build artifacts if any (though Go builds in Docker)
# main
# *.exe

# Ignore uploads directory if it's mounted as a volume (already in Dockerfile COPY)
# uploads/
